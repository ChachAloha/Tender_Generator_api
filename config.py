import os
from dotenv import load_dotenv

# 加载环境变量
load_dotenv()

class Config:
    # OpenAI API配置
    OPENAI_API_KEY: str = os.getenv("OPENAI_API_KEY", "sk-d44269bc154944f0b35891086038c2d0")
    OPENAI_MODEL: str = os.getenv("OPENAI_MODEL", "deepseek-chat")
    OPENAI_BASE_URL: str = os.getenv("OPENAI_BASE_URL", "https://api.deepseek.com/v1")
    
    # 文档处理配置
    MAX_CHUNK_SIZE: int = int(os.getenv("MAX_CHUNK_SIZE", "2048"))
    CHUNK_OVERLAP: int = int(os.getenv("CHUNK_OVERLAP", "200"))
    
    # 并发配置
    MAX_CONCURRENT_REQUESTS: int = int(os.getenv("MAX_CONCURRENT_REQUESTS", "10"))
    
    # 任务配置
    TASK_TIMEOUT: int = int(os.getenv("TASK_TIMEOUT", "3600"))  # 任务超时时间(秒)
    TASK_RESULT_TTL: int = int(os.getenv("TASK_RESULT_TTL", "86400"))  # 任务结果保存时间(秒)
    
    # 服务配置
    HOST: str = os.getenv("HOST", "0.0.0.0")
    PORT: int = int(os.getenv("PORT", "8000"))
    DEBUG: bool = os.getenv("DEBUG", "false").lower() == "true"
    
    # 上传文件配置
    UPLOAD_DIR: str = os.getenv("UPLOAD_DIR", "./uploads")
    OUTPUT_DIR: str = os.getenv("OUTPUT_DIR", "./output")

    # 新增：日志配置
    APP_ENV: str = os.getenv("APP_ENV", "development") # "development" 或 "production"

class Prompts:
    # 文档总结提示
    SUMMARIZE_CHUNK = """
作为一名专业的投标人，请对招标文件中技术标要求进行分析，为后续的技术标制作做准备。


待总结的标书内容：
---
{chunk}
---

请提供你的专业总结：
"""
    # 新增：投标分析报告生成提示
    GENERATE_FINAL_SUMMARY = """
请将之前招标文件信息中关于技术标制作方面的要求列出。

**各部分总结内容如下:**
---
{combined_summaries}
---

请生成最终的技术标编写要求：
"""

    # 目录生成提示
    GENERATE_OUTLINE = """
作为一名投标人，请根据招标文件要求和以下招标分析报告，生成一个专业的、结构丰富的技术标目录。

**特别强调：**
- **第3-4级目录必须尽可能详细和丰富**，每个第2级章节下应有**至少3-5个**第3级子章节
- 每个第3级章节下应有**至少2-4个**第4级子章节（如果目录深度要求到第4级）
- 第3-4级的内容要具体、细化，避免过于宽泛的标题

**具体要求：**
1. **目录结构规范：** 必须符合招标文件技术要求，包含技术方案要中的评分部分所有内容，但不限于。
2. **评分导向设计：** 根据招标要求的评分点设计章节。
3. **树状结构要求：** 
   - 每个一级章节下应包含**3-5个**二级章节
   - 每个二级章节下应包含**3-5个**三级章节
   - 如果需要第4级，每个三级章节下应包含**2-4个**四级章节
   - **严禁**线性结构（每层只有一个子节点）
4. **层级深度：** 必须精确到 **{max_level}** 层，且每个分支都要充分展开到要求的深度。
5. **标题规范：** 
   - 标题不含编号（如"第一章"、"1.1"、"1.1.1"）
   - 避免使用"其他"、"相关内容"等模糊表述
6. **必须返回纯JSON格式**

**示例说明：**
- 第3级标题应该具体且可执行，能够明确指导内容的编写方向
- 第4级标题应该是第3级内容的进一步细化和分解
- 避免使用过于宽泛或模糊的表述

**输出JSON格式要求：**
{json_example}

**招标分析报告内容：**
{summary_content}

请按照招标文件要求，生成一个层级深度为 **{max_level}** 层、第3-4级内容丰富详细、结构充实的技术标目录，不涉及商务标和报价，并直接返回JSON格式。
"""

    # 目录生成提示（仅三级）
    GENERATE_OUTLINE_3 = """
作为一名投标人，请根据招标文件要求和以下招标分析报告，生成一个专业的、结构丰富的技术标目录。

**特别强调：**
- 只生成三级目录（level=1/2/3），严禁出现第4级或更深层级。
- 每个第2级章节下应有**至少3-5个**第3级子章节。
- 第3级标题必须具体、可执行，避免宽泛表述。

**具体要求：**
1. **目录结构规范：** 必须符合招标文件技术要求，覆盖评分点相关内容。
2. **评分导向设计：** 以评分点为导向组织章节。
3. **树状结构要求：**
   - 每个一级章节下应包含**3-5个**二级章节
   - 每个二级章节下应包含**3-5个**三级章节
   - **严禁**线性结构（每层只有一个子节点）
4. **层级深度：** 必须精确到 **3** 层，且第3级节点的 `children` 必须为空数组。
5. **标题规范：**
   - 标题不含编号（如"第一章"、"1.1"、"1.1.1"）
   - 避免使用"其他"、"相关内容"等模糊表述

**示例说明：**
- 第3级标题应该具体且可执行，能够明确指导内容的编写方向
- 避免使用过于宽泛或模糊的表述

**输出JSON格式要求：**
{json_example}

**招标分析报告内容：**
{summary_content}

请按照招标文件要求，生成一个层级深度为 **3** 层且第3级内容丰富详细、结构充实的技术标目录，不涉及商务标和报价，并直接返回JSON格式。
"""

    # 目录生成提示（仅四级）
    GENERATE_OUTLINE_4 = """
作为一名投标人，请根据招标文件要求和以下招标分析报告，生成一个专业的、结构丰富的技术标目录。

**特别强调：**
- 必须生成四级目录（level=1/2/3/4）。
- 每个第2级章节下应有**至少3-5个**第3级子章节。
- 每个第3级章节下应有**至少2-4个**第4级子章节。
- 第3-4级标题必须具体、可执行，避免宽泛表述。

**具体要求：**
1. **目录结构规范：** 必须符合招标文件技术要求，覆盖评分点相关内容。
2. **评分导向设计：** 以评分点为导向组织章节。
3. **树状结构要求：**
   - 每个一级章节下应包含**3-5个**二级章节
   - 每个二级章节下应包含**3-5个**三级章节
   - 每个三级章节下应包含**2-4个**四级章节
   - **严禁**线性结构（每层只有一个子节点）
4. **层级深度：** 必须精确到 **4** 层，且每个第3级节点均应包含第4级子节点。
5. **标题规范：**
   - 标题不含编号（如"第一章"、"1.1"、"1.1.1"）
   - 避免使用"其他"、"相关内容"等模糊表述

**示例说明：**
- 第3级标题应该具体且可执行，能够明确指导内容的编写方向
- 第4级标题应该是第3级内容的进一步细化和分解
- 避免使用过于宽泛或模糊的表述

**输出JSON格式要求：**
{json_example}

**招标分析报告内容：**
{summary_content}

请按照招标文件要求，生成一个层级深度为 **4** 层且第3-4级内容丰富详细、结构充实的技术标目录，不涉及商务标和报价，并直接返回JSON格式。
"""
    
    # 内容生成提示
    GENERATE_SECTION_CONTENT = """
请根据以下信息，生成一段完整、专业、详实的章节内容：

章节标题：{section_title}
章节级别：第{section_level}级
{context_info}

文档总结概要：
---
{summary}
---

内容要求：
1. 内容专业、严谨、逻辑清晰、信息丰富
2. 根据章节级别调整内容深度和详细程度：
   - Level 3（三级章节）：
     * 字数要求：3000-4000字
     * 内容结构：5-7个段落，每段落3000-4000字
   - Level 4（四级章节）：
     * 字数要求：3000-4000字
     * 内容结构：4-6个段落，每段落3000-4000字
3. 内容要与文档总结和章节标题紧密相关
4. 要以投标人的口吻进行编写
5. 作为投标人需要有自己的实施方法和管理措施


输出格式要求（重要）：
1. 使用"<p>"和"</p>"标记每个段落的开始和结束
2. 每个段落应该是完整的一段话，不要在段落中间换行
3. 段落之间不要有空行，直接使用</p><p>连接
4. 不要使用任何其他格式标记（如Markdown、HTML等）
5. 不要输出列表、表格、图表等特殊内容
6. 每个段落应该有实质内容，避免过短的段落

示例格式：
<p>这是第一个段落的内容，包含完整的句子和观点。整个段落是连续的文字，没有中间换行。段落内容丰富详实，包含具体的技术细节或实施方案。</p><p>这是第二个段落的内容，与第一段之间没有空行。每个段落都有明确的主题和充实的内容，详细阐述相关的内容。</p><p>这是第三个段落，继续深入探讨相关内容，提供更多的实施细节和专业建议。</p>

请严格按照上述格式生成内容：
"""

    # 新增：续写内容提示
    CONTINUE_SECTION_CONTENT = """
作为投标人，请在已有内容的基础上，进行深入的续写和扩充，提供更多细节和内容。

章节标题：{section_title}
章节级别：第{section_level}级
{context_info}

文档总结概要：
---
{summary}
---

**已有的章节内容：**
---
{existing_content}
---

**续写要求：**
1.  **无缝衔接**：续写内容必须与已有内容的风格、逻辑和术语保持一致，确保阅读流畅。
2.  **深度扩展**：不要简单重复已有信息。你应该：
    *   在已有内容基础上增加新的内容以更好的阐述。
3.  **保持专业**：使用专业、严谨的语言。
4.  **内容丰富**：生成足够丰富的内容，至少是已有内容长度的80%以上。
5.  **字数与结构**：
    *   续写内容需生成 **3000-4000字**。
    *   续写内容应包含 **4-6个** 结构完整、内容充实的段落。

**输出格式要求（重要）：**
1.  **只输出续写的新内容**，不要重复已有内容。
2.  使用 **"<p>"和"</p>"** 标记每个段落。
3.  段落之间不要有空行，直接使用 `</p><p>` 连接。
4.  不要使用任何其他格式标记。

请只针对 **续写部分**，严格按照要求生成内容：
"""

    # 新增：补充文档目录生成提示
    GENERATE_SUPPLEMENTARY_OUTLINE = """
你是一个专业的文档架构师。请根据以下项目总结和用户补充需求，生成一份用于补充说明的文档目录（Outline）。

**项目总结:**
---
{summary}
---

**用户补充需求:**
---
{user_request}
---

**目录生成要求:**
1.  **目标**: 生成一个结构清晰、重点突出的补充说明目录。
2.  **结构**: 必须包含一个总标题（title），以及一个'outline'列表。
3.  **层级**: 目录结构应为“三级标题”下包含“四级标题”的模式。具体如下：
    -   `level: 1`: 1个，作为文档主章节标题，例如 "XX项目补充说明材料"。
    -   `level: 2`: 1个，作为二级标题，总结补充内容的核心主题。
    -   `level: 3`: 1个，作为三级标题，是对核心主题的进一步细化。
    -   `level: 4`: 3至5个，作为四级标题，列出具体的补充要点。这些要点应直接响应用户的补充需求。
4.  **字数要求**：生成的总字数目标约为2000字，请在规划标题时考虑内容的深度和广度。
5.  **输出格式**: 必须严格遵循以下JSON格式，不要添加任何额外的解释或注释。

**JSON输出格式示例:**
```json
{{
    "title": "关于[项目名称]的补充说明",
    "outline": [
        {{
            "id": "sup_level1_1",
            "title": "补充说明材料",
            "level": 1,
            "children": [
                {{
                    "id": "sup_level2_1",
                    "title": "关于[核心主题]的详细说明",
                    "level": 2,
                    "children": [
                        {{
                            "id": "sup_level3_1",
                            "title": "[对核心主题的细化说明]",
                            "level": 3,
                            "children": [
                                {{
                                    "id": "sup_level4_1",
                                    "title": "[具体补充要点一]",
                                    "level": 4,
                                    "children": []
                                }},
                                {{
                                    "id": "sup_level4_2",
                                    "title": "[具体补充要点二]",
                                    "level": 4,
                                    "children": []
                                }},
                                {{
                                    "id": "sup_level4_3",
                                    "title": "[具体补充要点三]",
                                    "level": 4,
                                    "children": []
                                }}
                            ]
                        }}
                    ]
                }}
            ]
        }}
    ]
}}
```

请立即生成JSON格式的目录：
"""

    # 新增：补充文档章节内容生成提示
    GENERATE_SUPPLEMENTARY_SECTION_CONTENT = """
你是一名专业的文档撰写专家。请根据提供的项目背景、用户需求和章节信息，为以下章节撰写详细内容。

**项目背景和用户需求:**
---
{context}
---

**当前章节信息:**
- 章节标题: "{section_title}"
- 章节路径: "{full_path}"

**撰写要求:**
1.  **内容详实**: 围绕章节标题展开，提供具体信息、分析或解决方案。
2.  **字数适中**: 生成 **400-600字** 的内容。
3.  **逻辑清晰**: 内容需包含 **2-3个** 结构完整、论述清晰的段落。
4.  **专业严谨**: 使用专业、客观的语言。

**输出格式要求（重要）:**
- **只输出正文内容**，不要包含标题。
- 使用 **"<p>"和"</p>"** 标记每个段落。
- 段落之间不要有空行，直接使用 `</p><p>` 连接。
- 不要使用任何其他格式标记。

请严格按照要求生成章节内容：
"""

# 创建配置实例
config = Config()
prompts = Prompts()

# 验证必要配置
def validate_config():
    if not config.OPENAI_API_KEY:
        raise ValueError("OPENAI_API_KEY is required. Please set it in environment variables.")
    
    # 创建必要的目录
    os.makedirs(config.UPLOAD_DIR, exist_ok=True)
    os.makedirs(config.OUTPUT_DIR, exist_ok=True) 